-r requirements.txt

# Dependencies for --mode local (default).
yt-dlp>=2024.1.1
faster-whisper>=1.0.0
opencv-python>=4.8.0
ollama>=0.3.0

# Optional: only needed if you set LLM_PROVIDER=openai or gemini
# openai>=1.0.0
# google-genai>=1.0.0

# torch is only needed if you want CUDA Whisper. CPU works without it.
# torch>=2.0
